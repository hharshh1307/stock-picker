{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn api_server:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/discovery/market-pulse",
    "restartPolicyType": "ON_FAILURE"
  }
}
